# 工作流程

在實務中，你可以將 Quarto 搭配 **GitHub Actions**，自動生成：

- 靜態網站
- PDF 書籍
- 技術文件

例如：


```bash
quarto render
```


測試程式碼區塊：C#。


```csharp
string name = "mike";
int age = 20;
var tuple = (name, age); // 自動推導元素名稱
Console.WriteLine($"{tuple.name} {tuple.age}");
```

